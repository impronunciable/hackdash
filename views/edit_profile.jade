.span7.offset1
	h3 Edit Your Profile

		if((typeof errors != "undefined") && errors.length)
			p An error ocurred:
			each err in errors
				p(style="margin-left:20px") #{err}

		form.ajaxForm.form-horizontal(method="POST", action="/api/users/" + user.id)
			.control-group
				input(name="name", type='text', placeholder='Nombre', value=user.name)
			.control-group
				input(name="email", type='text', placeholder='Email', value=user.email)
			.control-group
				textarea(name="bio", placeholder='Algo sobre vos') #{user.bio}
			.control-group
				button.btn.btn-success(type="submit") Guardar cambios
				a.cancel.btn(data-dismiss="modal") Cancelar
