
<div class="modal-header">
  <button type="button" data-dismiss="modal" aria-hidden="true" class="close">×</button>
  <h3>Log in</h3>
</div>
<div class="row">
  <div class="span4 offset1">
    {{#each providers}}
    <a href="/auth/{{.}}" class="btn btn-large signup-btn signup-{{.}}" data-bypass>
      <i></i>Access with {{firstUpper .}}
    </a>
    {{/each}}
  </div>
</div>